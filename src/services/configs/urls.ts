const baseURL = "http://jsonplaceholder.typicode.com/";
const baseCarsURL = "http://owu.linkpc.net/carsAPI/v1";

const urls = {
  users: "/users",
  comments: "/commens",
};

const carsUrls = {
  cars: "/cars",
};

export { baseURL, baseCarsURL, urls, carsUrls };
