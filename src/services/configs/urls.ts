const baseUsersURL = "http://jsonplaceholder.typicode.com/";
const baseCarsURL = "http://owu.linkpc.net/carsAPI/v1";

const usersURLs = {
  users: "/users",
  comments: "/commens",
  posts: "/posts",
};

const carsURLs = {
  cars: "/cars",
};

export { baseUsersURL, baseCarsURL, usersURLs, carsURLs };
